#include "main.h"
/**
 * *_strncpy - copies a string
 * @src: our parameter
 * @dest: another parameter
 * @n: another one
 * Return: the string copied
 */
char *_strncpy(char *dest, char *src, int n)
{
	int i = 0;
	int n = 0;

	while (dest[i] != '\0' && src[n] != '\0')
	dest[i] = src[j]
	i++;
	j++;
	}
	return (dest);
}

