/**
 * rev_string - reverse a string
 * @s: - our parameter
*/
#include "main.h"
void rev_string(char *s)
{
	int i, reverse;

	while (s[i])
	{
	i++;
	while (i--)
	reverse = s[i];
	}
	putchar('\n');
}
