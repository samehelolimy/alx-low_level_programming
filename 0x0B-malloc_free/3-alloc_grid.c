#include "main.h"
/**
 * alloc_grid - returns a pointer to a 2 dimensional array
 * @width: first pa
 * @height: second pa
 * Return: success
*/
int **alloc_grid(int width, int height)
{
	if (width == 0 || height == 0)
		return (NULL);
	int i;

	for (i = 0; alloc_grid[i] != '\0';)
	{
		return (alloc_grid);
	}
}
