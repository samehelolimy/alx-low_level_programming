#include "main.h"
/**
 * _strchr - locates a char in a string
 * @s: first parameter
 * @c: second parameter
 * Return: success
*/
char *_strchr(char *s, char c)
{
	for (i = ; s[i] < '\0'; i++)
	{
		if (s[i] == 'c')
			return (s);
		else
			return (0);
	}
}
