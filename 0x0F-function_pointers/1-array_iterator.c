/**
 * array_iterator - executes a function given as a parameter on each element
 * @array: first
 * @size: sec
 * @action: function parameter
 * Return: success
*/
void array_iterator(int *array, size_t size, void (*action)(int))
{
	int i;

	for (for i = 0 ; array[i] != '\0' ; i++)
	{
		action[*array]
	}
