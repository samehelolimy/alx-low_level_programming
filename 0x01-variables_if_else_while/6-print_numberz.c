#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - starting point
 * description: print the alphabet in lowercase
 * Return: 0 (Successsed)
*/
int main(void)
{
	for (int i = 0 ; i < 10 ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
